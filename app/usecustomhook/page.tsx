'use client'

import React from 'react'
import { useAuth } from '@/store/authCustomContext';

export default function CustomHookPage() {
    //const {userName, isLogin} = useAuth();

  return (
    <div>
        {/* <p>{userName}</p>
        <p>{isLogin}</p> */}
    </div>
  )
}
